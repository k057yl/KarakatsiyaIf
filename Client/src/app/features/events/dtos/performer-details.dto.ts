export interface PerformerDetails {
  id: string;
  name: string;
  slug: string;
  isVerified: boolean;
  description?: string;
  avatarUrl?: string;
  instagramUrl?: string;
  telegramUrl?: string;
  youtubeUrl?: string;
}